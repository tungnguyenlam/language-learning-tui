# Fixtures

Fixtures are stable examples for tests and future agent work.

- `internal/content/testdata/anki/`: Anki-friendly TSV import/export examples.
- `internal/tui/testdata/`: future TUI golden snapshots.

When changing parser behavior, add or update a fixture instead of relying only on inline test strings.
