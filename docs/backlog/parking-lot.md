# Parking Lot

- Local LLM provider.
- Cloud or multi-device sync (local progress backup/restore now lives on Import/Export).
- Richer cloze deletion authoring beyond the dictionary cloze quick-add.

Removed as shipped: `.apkg` import/export, audio pronunciation prompts, grammar drill mode
(the Practice Hub trainers), local SQLite progress backup/restore.
