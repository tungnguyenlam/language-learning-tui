# Parking Lot

- Local LLM provider.
- Sync or backup workflow (export/restore the SQLite progress database).
- Richer cloze deletion authoring beyond the dictionary cloze quick-add.

Removed as shipped: `.apkg` import/export, audio pronunciation prompts, grammar drill mode
(the Practice Hub trainers).
