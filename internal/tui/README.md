# internal/tui

Bubble Tea shell and responsive views.

This package owns terminal layout, keyboard input, mouse routing, and rendering. Domain logic should stay in `internal/core`; persistence should stay in `internal/storage`.
